<div class="<%= block.data.classes %><% if block.data.block != 1 %> block-small-typography <% end %>">
  <% if block.data.block == 1 %>
  <h1 class="textbar title"><%= block.title %></h1>
  <% elsif %>
  <h1 class="textbar textbar-inverse title sub-title"><%= block.title %></h4>
  <% end %>
  <div><%= block.body %></div>
  <div>
    <% if block.data.references %>
    <% for reference in block.data.references %>
      <%= partial(:"partials/story-reference", :locals => { :reference => reference })  %>
    <% end %>
    <% end %>
  </div>
</div>